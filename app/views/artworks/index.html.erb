<% @artworks.each do |artwork| %>
    <h1>Artwork Title: <%= artwork.title %></h1>
    <p>Artist: <%= artwork.artist %></p> 
    <%= button_to "Delete Museum :(", museum_artwork_path(@museum, artwork), method: :delete %>
<% end %>


