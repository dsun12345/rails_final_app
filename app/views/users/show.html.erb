<h1>List of Museums!</h1>
<% @user.museums.uniq.each do |museum| %>
    <h2><%= museum.name %></h2>
    <p><%= link_to "Click above to view artworks within", museum_artworks_path(museum)%></p>
<% end %>

<%= link_to "Add a Museum to Store the Artwork!!", new_museum_path %>